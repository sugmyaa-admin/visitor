import { SpinDirective } from './spin.directive';

describe('SpinDirective', () => {
  it('should create an instance', () => {
    const directive = new SpinDirective();
    expect(directive).toBeTruthy();
  });
});
